# Base URL of the CloudOptima API deployed on Render.
# Local development against a locally running backend:
VITE_API_BASE_URL=http://localhost:8000
# Production (set this in the Vercel project's Environment Variables):
# VITE_API_BASE_URL=https://cloud-resource-optimizer-api.onrender.com
